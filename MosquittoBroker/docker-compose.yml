version: '3.7'

services:
  mosquitto:
    image: eclipse-mosquitto
    ports:
      - 1884:1884
    volumes:
      - ./conf:/mosquitto/config
      - ./data:/mosquitto/data
      - ./log:/mosquitto/log