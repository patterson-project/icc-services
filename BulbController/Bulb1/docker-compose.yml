version: '3.7'

services:

  bulb-1-controller:
    image: canadrian72/iot-control-center:bulb-1-controller
    build:
      context: ../
      dockerfile: $PWD/Dockerfile
    ports:
      - "8000:8000"
